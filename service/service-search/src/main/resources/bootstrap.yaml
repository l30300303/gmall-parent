spring:
  application:
    name: service-search    # 当前服务的名称
  cloud:
    nacos:
      server-addr: 192.168.126.138:8848
      config:
        shared-configs:
          - common.yml
          - endpoint.yml